procs:
  infra:
    shell: "docker-compose -f docker-compose.dev.yml up"
    stop: SIGINT
    autostart: true
  web:
    cmd: ["mise", "x", "--", "npm", "run", "dev"]
    cwd: frontend
  worker:
    cmd: ["mise", "x", "--", "go", "run", "./cmd/janushc-dash"]

